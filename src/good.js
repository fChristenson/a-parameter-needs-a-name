const fizzBuzz = require('./func');

// thank you
const timesToRun = 100;
const timesToSayFizz = 100;
const timesToSayBuzz = 100;
const timesToSayFizzBuzz = 100;
const logFunc = console.log;

// copy paste the names from the function declaration and add them like this,
// it helps a lot when the next person comes to your code
fizzBuzz(timesToRun, timesToSayFizz, timesToSayBuzz, timesToSayFizzBuzz, logFunc);